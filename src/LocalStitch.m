function [newImg] = LocalStitch(data,i,j,N,M,lastImg)
    %Here we intake our data and stitch together surounding data
    newImg = lastImg;
    %First collect needed additions
    if i == 1
        %Along the top

    elseif i == N
        %Along the bottom
    else
        %In space
    end
end